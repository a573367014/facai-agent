# @agent/contracts

前后端共享的传输层契约，只描述 HTTP/SSE 边界，不包含数据库记录、业务服务或运行时实现。

约束：

- 对外类型使用 `Dto` 后缀，避免与 API 内部的 `Record` 混淆。
- API 必须显式把内部模型映射为 DTO，不能直接把数据库记录透传给 Web。
- 本包只包含类型；请求校验仍在服务端 HTTP 模块完成，避免给浏览器增加不必要的运行时代码。
- 业务模块不能依赖 Web 或 API 的内部文件。
